# ─────────────────────────────────────────────────────────────────
# Holodeck local environment template.
#
# Copy to `.env` (gitignored) and fill in the values, then `npm start`.
# start-web.js loads this file and FAILS FAST if a required var is
# missing, so a misconfigured stack dies at boot instead of serving a
# half-broken app (get-session 404 / rest/v1 401). Real environment
# variables (e.g. Heroku config vars) always win over this file.
# ─────────────────────────────────────────────────────────────────

# ── Required: Postgres + auth ──────────────────────────────────────

# Postgres connection string. MUST include a login role in the userinfo
# (postgresql://USER@host/db) — PostgREST SET ROLEs into the token's
# `role` claim, which the auth-shim derives from this URI. Without a
# role, every authenticated /rest/v1 call is rejected as anonymous.
#
# DATA STORE INVARIANT: this MUST be the Heroku Postgres connection string
# (heroku config:get DATABASE_URL). Heroku Postgres is the ONE data store
# for all project data. Neon is AUTH-ONLY (see NEON_AUTH_BASE / NEON_JWKS_URL
# below) — never point DATABASE_URL at a Neon host. start-web.js refuses to
# boot on a deployed dyno (or when HOLO_REQUIRE_HEROKU_PG=1) if DATABASE_URL
# is a Neon host, to prevent the app from silently reading/writing the wrong
# database. Local dev may use localhost (role = your OS/db user):
DATABASE_URL=postgresql://YOUR_DB_ROLE@localhost/holodeck

# Set to 1 to make the "DATABASE_URL must be Heroku Postgres, not Neon" check
# fatal even outside a Heroku dyno (recommended for any shared/staging run).
# HOLO_REQUIRE_HEROKU_PG=1

# HS256 secret shared by the auth-shim (signs minted tokens) and
# PostgREST (verifies them). Any sufficiently random string works
# locally as long as both use the same one — start-web.js passes this
# single value to both children. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=

# Neon Auth (Better Auth) origin and JWKS. Non-secret URLs from your
# Neon project console (Auth tab). The /auth/** proxy is only registered
# when NEON_AUTH_BASE is set; NEON_JWKS_URL is derived from it if omitted.
NEON_AUTH_BASE=https://<endpoint>.neonauth.<region>.aws.neon.tech/neondb/auth
NEON_JWKS_URL=https://<endpoint>.neonauth.<region>.aws.neon.tech/neondb/auth/.well-known/jwks.json

# ── Required for the AI features: Gemini proxy ─────────────────────

# Gemini Developer API key. Used by the /api/gemini/* proxy in server.js
# (e.g. "Generate script with Gemini"). Without it those routes 500.
GEMINI_API_KEY=

# ── Optional: Aubrey Demo shared-key proxy ─────────────────────────

# One shared API key per Aubrey app, held server-side. The /api/aubrey/*
# proxy in server.js forwards to Aubrey with the key attached and the
# signed-in user's email as ?email= ("the key owner acts on behalf of
# the email user"). This is a FALLBACK: SEs who set their own per-device
# key in the builder UI still call Aubrey directly. Each key is optional
# — an unset key just means that app's shared path is unavailable (its
# routes 503 and the UI doesn't offer the keyless pull). NOT validated at
# boot (like GEMINI_API_KEY), so the stack starts fine without them.
# AUBREY_POCKETSIC_KEY=psk_...
# AUBREY_SCRIPTWRITER_KEY=dsw_...
# AUBREY_BRANDKIT_KEY=bkit_...
# Base URLs default to https://{pocketsic,scriptwriter,brandkit}.aubreydemo.com.
# Override only if the hosts differ (brand-kit host is TBC):
# AUBREY_POCKETSIC_BASE=https://pocketsic.aubreydemo.com
# AUBREY_SCRIPTWRITER_BASE=https://scriptwriter.aubreydemo.com
# AUBREY_BRANDKIT_BASE=https://brandkit.aubreydemo.com

# ── Optional ───────────────────────────────────────────────────────

# GEMINI_TEXT_MODEL=gemini-3.5-flash
# GEMINI_IMAGE_MODEL=gemini-3-pro-image

# Google Cloud Storage for uploaded images/logos (server.js sign/proxy).
GCS_BUCKET=holodeck_images
GOOGLE_APPLICATION_CREDENTIALS=./holodeck-gcs-key.json

# Google Slides export ("Create Google Slides" button, server.js /api/slides/*).
# The JSON CONTENTS of the OAuth client you downloaded from Google Cloud
# Console (the guide's credentials.json — Desktop or Web application type).
# On one line, e.g. GOOGLE_OAUTH_CLIENT_JSON={"web":{"client_id":"...","client_secret":"..."}}
# Add <origin>/api/slides/oauth/callback as an Authorized redirect URI on
# that OAuth client (e.g. http://localhost:4173/api/slides/oauth/callback).
# Per-user refresh tokens are stored in the same private GCS bucket under
# oauth/ (falls back to in-memory if GCS is unconfigured). Leave unset to
# hide the button — the stack boots fine without it.
GOOGLE_OAUTH_CLIENT_JSON=
